export const isLogin = () => {
  return {
    type: "LOGIN"
  }
}

export const isRegister = () => {
  return {
    type: "REGISTER"
  }
}

export const isClose = () => {
  return {
    type: "CLOSE"
  }
}
